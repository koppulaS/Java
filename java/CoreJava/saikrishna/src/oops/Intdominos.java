package oops;

public interface Intdominos {
	//abstract method
    public abstract void pizza();   //this we call as
 //in interface if we don't write public it is correct because it will take it as default case//
 //  in interface we can take the value of public,abstract,private,default,static,strict fp are permitted//
    //in interface final value is not permitted//
    //if we use private we need body means= {}//
    //abstract method doesn't have method body//
}
